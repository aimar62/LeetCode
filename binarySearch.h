#include "stdio.h"
#include "stdlib.h"
#include "string.h"
#include "stdbool.h"

#ifndef _BINARY_TREE_H_
#define _BINARY_TREE_H_

extern int findRightSpace(int* in, int len, int target);
extern int findLeftSpace(int* in, int len, int target);
#endif